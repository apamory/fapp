<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.favorites">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AnimalsActivity"></activity>
        <activity android:name=".DesignersActivity" />
        <activity android:name=".FoodActivity" />
        <activity android:name=".RestActivity" />
        <activity android:name=".StoresActivity" />
        <activity android:name=".CarsActivity" />
        <activity android:name=".ShowsActivity" />
        <activity android:name=".MoviesActivity" />
        <activity android:name=".SportsActivity" />
        <activity android:name=".ColorsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>